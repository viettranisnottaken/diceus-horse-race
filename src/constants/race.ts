export const TOTAL_HORSES_COUNT = parseInt(import.meta.env.VITE_TOTAL_HORSES_COUNT)
export const HORSES_COUNT_PER_ROUND = parseInt(import.meta.env.VITE_HORSES_COUNT_PER_ROUND)
export const ROUNDS_COUNT = parseInt(import.meta.env.VITE_ROUNDS_COUNT)
export const BASE_SPEED = parseInt(import.meta.env.VITE_BASE_SPEED)
